ls:
  .dir: regex:([.a-z0-9]*)([-.][a-z0-9]+)*
  .css: regex:([.a-z0-9]*)([-.][a-z0-9]+)*
  .ts: regex:([.a-z0-9]*)([-.][a-z0-9]+)*
  .tsx: regex:([.a-z0-9]*)([-.][a-z0-9]+)*

ignore:
  - .git
  - .idea
  - node_modules
  - dist
  - storybook-static
  - public
  - .storybook
  - src/assets